import React from 'react';
import Home from './pages/Home';

/**
 * App is a thin composition layer.
 * All logic lives in pages/ services/ and workers/.
 */
const App: React.FC = () => <Home />;

export default App;
